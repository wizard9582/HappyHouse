<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- name space: 쿼리를 구별하기 위한 키워드 -->
<mapper namespace="com.happyhouse.backend.model.repo.AttLocRepo">
	<select id="getUser" parameterType="Attention" resultType="AttLoc">
		select * 
		from commercialarea 
		where gu = #{gu} and dong = #{dong} and codename1 = 음식
	</select>
</mapper>